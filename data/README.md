# Dataset and data layout

Raw downloaded media and original metadata live in `raw/videos/`. Derived
audio and typed transcripts live in `processed/`. Claim, evidence, and result
directories are reserved for later pipeline stages.

The current manifest contains 25 candidate short-form health videos. Large
media files are intentionally excluded from version control.
